{
    "buildCommand": "cd ../.. && npm install -g pnpm@9 && pnpm --filter @remote-app/shared build && pnpm --filter @remote-app/client build",
    "outputDirectory": "dist",
    "installCommand": "cd ../.. && npm install -g pnpm@9 && pnpm install --no-frozen-lockfile",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "env": {
        "VITE_SERVER_URL": "https://kodokremote.zeabur.app"
    }
}